# Sample workflow for building and deploying a Hugo site to GitHub Pages
name: Deploy Hugo site to Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0
      
      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: '0.110.0'
          extended: true

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          
      - name: Install dependencies
        run: |
          echo "Checking file structure..."
          ls -la themes/tella/assets/css/
          echo "Main CSS content:"
          cat themes/tella/assets/css/main.css
          echo "Cleaning npm cache..."
          npm cache clean --force
          echo "Installing dependencies..."
          npm ci
          echo "Checking installed versions:"
          npm list tailwindcss
          npm list @tailwindcss/typography
          
      - name: Build CSS
        env:
          NODE_ENV: production
          DEBUG: "*"
        run: |
          echo "Building CSS..."
          ./node_modules/.bin/tailwindcss -i ./themes/tella/assets/css/main.css -o ./themes/tella/assets/css/style.css --minify || {
            echo "CSS build failed. Trying alternative build..."
            npx -p tailwindcss@3.4.1 tailwindcss -i ./themes/tella/assets/css/main.css -o ./themes/tella/assets/css/style.css --minify
          }
          echo "CSS build complete. Checking output:"
          ls -la themes/tella/assets/css/

      - name: Build Hugo
        env:
          HUGO_ENVIRONMENT: production
          HUGO_VERSION: '0.110.0'
        run: |
          echo "Building Hugo site..."
          hugo version
          HUGO_ENV=production hugo --gc --minify --baseURL "${{ steps.pages.outputs.base_url }}/"

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-24.04
    needs: build
    permissions:
      pages: write
      id-token: write
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4 

